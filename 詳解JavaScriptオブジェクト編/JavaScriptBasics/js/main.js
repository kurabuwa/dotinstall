'use strict';
{
  // const player = ['taguchi',32];
  const player = {
    name: 'tagichi',
    score: 32,
  };
// console.log(player.name);
// player.score = 100;
// console.log(player);

delete player.score;
console.log(player);
}
